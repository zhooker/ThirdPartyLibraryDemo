<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thirdparty">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".rxjava.RxJavaActivity" />
        <activity android:name=".rxjava.rxjava2.RxJavaAPIActivity" />
        <activity android:name=".hybrid.JSInterfaceActivity" />
        <activity android:name=".hybrid.JSBridgeActivity" />
        <activity android:name=".retrofit.download.RetrofitDownloadActivity" />
        <activity android:name=".rxjava.rxbus.RxBusActivity" />
        <activity android:name=".permission.PermissionActivity"></activity>
    </application>

</manifest>